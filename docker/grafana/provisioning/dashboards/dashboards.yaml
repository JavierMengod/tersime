apiVersion: 1

providers:
  - name: tersime
    folder: Tersime
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
